# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET        /ping                controllers.Application.firstAsyncAction
GET        /worker/:msg         controllers.Application.workerAdd(msg)
